sap.ui.define([
   "sap/ui/core/mvc/Controller",
], (Controller) => {
   "use strict";
   const CONTROLLER_NAME = "ui5.testinvent.controller.App";

   return Controller.extend(CONTROLLER_NAME, {
     
   });
});